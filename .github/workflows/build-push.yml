name: Build & Push to GHCR

on:
  push:
    branches:
      - deploy

jobs:
  build-push:
    uses: sward-UPC/.github/.github/workflows/build-push-ghcr.yml@main
    with:
      image_name: sward-lambda-interacciones
      service_type: lambda
      aws_service_name: sward-lambda-interacciones
      needs_shared: false
    secrets: inherit
